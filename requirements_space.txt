# HF Space requirements — Python 3.12, spaces GPU
# ─────────────────────────────────────────────────────────────
# torch ships with CUDA libs needed by some inference paths.
# Add torch first from PyTorch index, then the rest.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.8.0

# spaces — HF Spaces runtime (gradio==5.0.0 is SDK-forced, do NOT pin another version)
# Gradio 5/6 backward compat handled at runtime in app_space.py via try/except
spaces

# VLM inference (OpenAI-compatible client for HF Router)
openai>=1.0

# Modal endpoint caller (Gradio API for VLM inference via Modal)
requests

# Image processing
pillow
numpy

# CVD simulation (local, CPU)
daltonlens

# Env management — huggingface_hub must be <0.26 (HfFolder removed in 0.26)
# gradio 5.x depends on HfFolder which was removed in huggingface_hub 0.26
python-dotenv
huggingface_hub==0.25.2

# Optional: for local development only (not needed on Space)
# playwright  (commented — no browser needed on Space, screenshot is file upload)
# pytest-playwright