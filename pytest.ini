[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning

markers =
    smoke: quick import/build checks — no network, no GPU
    pipeline: full URL→report pipeline with mocked VLM
    slow: real VLM API calls — needs HF_TOKEN set